FROM nginx:alpine

# Copy custom nginx configuration for serving package metadata
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Create directory for package metadata
RUN mkdir -p /usr/share/nginx/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
